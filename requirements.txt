# Production dependencies for Astrophotography Data Management
# Install with: make deps

# CLI and configuration
click>=8.0.0
pyyaml>=6.0

# Database operations (sqlite3 is built into Python)
# Additional database utilities if needed
# sqlalchemy>=2.0.0